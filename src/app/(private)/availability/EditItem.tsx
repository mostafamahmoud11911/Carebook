import { Availability } from '@/types/types'
import React from 'react'

export default function EditItem({ availability }: { availability: Availability }) {
    return (
        <div>EditItem</div>
    )
}
